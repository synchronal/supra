#!/usr/bin/env bash

source_env_if_exists $HOME/.envrc

export PG_ROOT="$(pwd)/.local/postgres"
export PGPORT=54777
export PGUSER=postgres
export PGDATABASE=supra_test

source_env_if_exists .local/envrc

